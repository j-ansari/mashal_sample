import 'package:flutter/material.dart';
import '../../utils/app_images.dart';
import 'app_gradient_image.dart';

Widget baseView(
  Widget widget,
  context, {
  bool? visibility,
  required VoidCallback onTap,
  required Size size,
}) {}
